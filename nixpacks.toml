[phases.setup]
nixPkgs = ["nodejs_20", "python312"]

[phases.install]
cmds = ["cd backend && npm install", "cd frontend && npm install"]

[phases.build]
cmds = ["cd backend && npx prisma generate", "cd frontend && npm run build"]

[phases.start]
cmd = "cd backend && npx prisma db push && npm start"
